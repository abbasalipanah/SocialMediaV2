import { PhaseShellPage } from "../shared/PhaseShellPage";

export default function OverviewPage() {
  return (
    <PhaseShellPage
      description="A cross-channel view for the selected Brand scope."
      title="Overview"
    />
  );
}
